[
 {
  inputFileName: './data/inputData.txt',
  inputSchema: 'schema {switch:string, level:string, degree:long, rating:double}',
  inputDelimiter: ' ',
  filterFunc: 'fn (r) r.degree >= 1 and r.rating <= 0.3',
  projectionList: ['degree','rating'],
  attributeKind: ['continuous', 'ordinal'],
  outputFileType: 'CSVMatrix',
  outputFileName: './data/output.mtx'
 }
]
