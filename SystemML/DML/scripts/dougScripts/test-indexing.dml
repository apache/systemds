

A = Rand(rows=10, cols=10, min=1.0, max=1.0);

# case no indices, no indices  
B = A[:,:];

# case expr
B[1,5:6] = A[i+2,:];

B[1:10,5:6] = A[i+2:i+4,4];

C[max(A):21,23:34] = B;

#[C,V,S] = foo(A,B,C);