foo = function(Double C) return ( Double  C)
{
	print("foo in test.dml in matMult is ~/packages", C);
	C = C + 2;
}

bar = function(Double C) return ( Double  C)
{
	print("bar in test.dml in matMult is ~/packages", C);
	C = C + 3;
}

baz = function(Double C) return ( Double  C)
{
	print("baz in test.dml in matMult is ~/packages", C);
	C = C + 4;
}