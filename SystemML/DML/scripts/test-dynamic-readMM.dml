matMult = function(Int dim, Int dim2) return ( Matrix[Double]  tempm)
{
	tempm = read( "llr_xfile", rows="dim", cols="dim2", format="text");
}
	



dim = 10;
dim2 = 10;

F = matMult(dim, dim2)
fsum = sum(F);
print("fsum is ", fsum);
#write ( F, "F.output", format="text");
