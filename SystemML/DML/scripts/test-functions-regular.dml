

matMult = function(Int dim, Matrix[Double] B) return ( Matrix[Double]  C)
{


	C= Rand(rows=10, cols=10);
	tempmSum = sum(C);
	print("tempmSum is " + tempmSum);
	i = 0;
	while (i < 1){
		i = i + 2;
	}	

	
}

input1 = read( "./scripts/llr_xfile", rows=10, cols=10, format="text");
input2 = read( "./scripts/llr_xfile", rows=10, cols=10, format="text");
D = 2.0;
dim = 10;

F = matMult(dim, input1)


print("D should be 2 " + D);
fsum = sum(F);
print("fsum is " + fsum);
#write ( F, "F.output", format="text");
