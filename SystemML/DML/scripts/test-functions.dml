

matMult = function(Double p, Matrix[Double] A, Matrix[Double] B) return ( Matrix[Double]  C, int p)
{
	C = A %*% B
	#C = bar(C)
	p = p + 1
	Csum = sum(C);
	print("Csum in matMult is " + Csum);
	i = 0;
	while (i < 1){
		i = i + 2;
	}	

	
}

bar = function(Matrix[Double] C) return ( Matrix[Double]  C)
{
	C = C + 2;
}	


input1 = Rand(rows=10, cols=10, min=1.0, max=1.0);
input2 = Rand(rows=10, cols=10, min=1.0, max=1.0);
D = 2.0;

[F,p] = matMult(D,input1,input2)
[F,p] = matMult(D,max(D), input2)


print("D should be 2 " + D);
print("p should be 3 " + p);
fsum = sum(F);
print("fsum is " + fsum);
#write ( F, "F.output", format="text");
