x = 0;
V = readMM("data/fileC", rows=3, cols=3, format="text");
W = readMM("data/fileB", rows=3, cols=3, format="text");
R = Rand(rows=1, cols=1, min=0, max=1, pdf="uniform", sparsity=1.0);

a = castAsScalar( R );
W = W + a;

if(a == 1)
{
	a = 0;
}
