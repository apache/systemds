# junit test class: dml.test.integration.functions.aggregate.MaxTest.java

$$readhelper$$

Vector = readMM("$$indir$$vector", rows=$$rows$$, cols=1, format="text");
VectorMax = max(Vector);
VectorMaxHelper = VectorMax * Helper;
writeMM(VectorMaxHelper, "$$outdir$$vector_max", format="text");

Matrix = readMM("$$indir$$matrix", rows=$$rows$$, cols=$$cols$$, format="text");
MatrixMax = max(Matrix);
MatrixMaxHelper = MatrixMax * Helper;
writeMM(MatrixMaxHelper, "$$outdir$$matrix_max", format="text");