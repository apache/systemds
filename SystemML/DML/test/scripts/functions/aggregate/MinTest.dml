# junit test class: com.ibm.bi.dml.test.integration.functions.aggregate.MinTest.java

$$readhelper$$

Vector = read("$$indir$$vector", rows=$$rows$$, cols=1, format="text");
VectorMin = min(Vector);
VectorMinHelper = VectorMin * Helper;
write(VectorMinHelper, "$$outdir$$vector_min", format="text");

Matrix = read("$$indir$$matrix", rows=$$rows$$, cols=$$cols$$, format="text");
MatrixMin = min(Matrix);
MatrixMinHelper = MatrixMin * Helper;
write(MatrixMinHelper, "$$outdir$$matrix_min", format="text");