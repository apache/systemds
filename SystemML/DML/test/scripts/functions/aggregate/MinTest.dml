# junit test class: dml.test.integration.functions.aggregate.MinTest.java

$$readhelper$$

Vector = readMM("$$indir$$vector", rows=$$rows$$, cols=1, format="text");
VectorMin = min(Vector);
VectorMinHelper = VectorMin * Helper;
writeMM(VectorMinHelper, "$$outdir$$vector_min", format="text");

Matrix = readMM("$$indir$$matrix", rows=$$rows$$, cols=$$cols$$, format="text");
MatrixMin = min(Matrix);
MatrixMinHelper = MatrixMin * Helper;
writeMM(MatrixMinHelper, "$$outdir$$matrix_min", format="text");