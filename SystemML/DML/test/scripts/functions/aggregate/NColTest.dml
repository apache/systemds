# junit test class: dml.test.integration.functions.aggregate.NColTest.java

$$readhelper$$

Vector = readMM("$$indir$$vector", rows=$$rows$$, cols=1, format="text");
VectorCols = ncol(Vector);
VectorColsHelper = VectorCols * Helper;
writeMM(VectorColsHelper, "$$outdir$$vector_cols", format="text");

Matrix = readMM("$$indir$$matrix", rows=$$rows$$, cols=$$cols$$, format="text");
MatrixCols = ncol(Matrix);
MatrixColsHelper = MatrixCols * Helper;
writeMM(MatrixColsHelper, "$$outdir$$matrix_cols", format="text");