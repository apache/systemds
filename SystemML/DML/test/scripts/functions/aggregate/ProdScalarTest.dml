# junit test class: com.ibm.bi.dml.test.integration.functions.aggregate.ProdTest.java

$$readhelper$$

scalar = $$scalar$$;

computation = prod(scalar);
ComputationHelper = computation * Helper;

write(ComputationHelper, "$$outdir$$computed", format="text");