# junit test class: dml.test.integration.functions.aggregate.LengthTest.java

$$readhelper$$

computation = rowSums($$scalar$$);

ComputationHelper = Helper * computation;

write(ComputationHelper, "$$outdir$$computed", format="text");