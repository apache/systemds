# junit test class: com.ibm.bi.dml.test.integration.functions.aggregate.SumTest.java

$$readhelper$$

Vector = read("$$indir$$vector", rows=$$rows$$, cols=1, format="text");
VectorSum = sum(Vector);
VectorSumHelper = VectorSum * Helper;
write(VectorSumHelper, "$$outdir$$vector_sum", format="text");

Matrix = read("$$indir$$matrix", rows=$$rows$$, cols=$$cols$$, format="text");
MatrixSum = sum(Matrix);
MatrixSumHelper = MatrixSum * Helper;
write(MatrixSumHelper, "$$outdir$$matrix_sum", format="text");