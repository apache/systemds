# junit test class: dml.test.integration.functions.aggregate.SumTest.java

$$readhelper$$

Vector = readMM("$$indir$$vector", rows=$$rows$$, cols=1, format="text");
VectorSum = sum(Vector);
VectorSumHelper = VectorSum * Helper;
writeMM(VectorSumHelper, "$$outdir$$vector_sum", format="text");

Matrix = readMM("$$indir$$matrix", rows=$$rows$$, cols=$$cols$$, format="text");
MatrixSum = sum(Matrix);
MatrixSumHelper = MatrixSum * Helper;
writeMM(MatrixSumHelper, "$$outdir$$matrix_sum", format="text");