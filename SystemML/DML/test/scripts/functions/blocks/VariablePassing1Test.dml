# JUnit test class: dml.test.integration.functions.blocks.VariableTest.java

A = read("$$indir$$a", rows=$$rows$$, cols=$$cols$$, format="text");
B = read("$$indir$$b", rows=$$rows$$, cols=$$cols$$, format="text");

x = sum(A);
D = A * x;

C = B * D;
B = B * D;

continue = true;
while(continue == true) {
	F = 3 * B;
	E = 3 * D;
	continue = false;
}

write(C, "$$outdir$$c", format="text");
write(F, "$$outdir$$f", format="text");
write(E, "$$outdir$$e", format="text");