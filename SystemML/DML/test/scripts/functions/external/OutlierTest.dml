outliers = externalFunction(Matrix[Double] D, Integer m, Integer k) 
return (Matrix[Double] C) 
implemented in (classname="dml.packagesupport.OutlierWrapper",execlocation="master")

M = read( "$$indir$$M", rows=$$rows$$, cols=$$cols$$, format="text");

k = 2;
m = 5;

o = outliers(M,m,k);

write(o, "$$outdir$$o", format="text");