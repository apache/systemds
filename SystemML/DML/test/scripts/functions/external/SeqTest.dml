seqMini = externalFunction(Matrix[Double] A, Double sup, Double max_level) 
return (Matrix[Double] B, Matrix[Double] C) 
implemented in (classname="dml.packagesupport.SequenceMinerWrapper",execlocation="master");
 
minsup = 0.004;
max_level=5.0;

M = read( "$$indir$$M", rows=$$rows$$, cols=$$cols$$, format="text");

[A,B] = seqMini(M,minsup,max_level);

write(A, "$$outdir$$fseq", format="text");
write(B, "$$outdir$$sup", format="text");
