A = Rand(rows=20,cols=1,min=0,max=0);
B = Rand(rows=20,cols=1);
dummy = Rand(rows=1,cols=1,min=1.0,max=1.0);

parfor( i in seq(2,10,2) )
{ 
   B[i,1] = dummy*i;
   A[i,1] = B[i,1];
}

#print(A);