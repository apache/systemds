A = Rand(rows=20,cols=20,min=0,max=0);
B = Rand(rows=20,cols=20);

parfor( i in 1:20 )
{ 
   A[i,] = A[i,]+2;
  
   if( 1>1 )
   { 
     parfor( j in 1:20 )
     {
        print(j); 
     }
   }
}

#print(A);