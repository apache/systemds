A = Rand(rows=20,cols=20,min=0,max=0);
B = Rand(rows=20,cols=20,min=0,max=0);
C = Rand(rows=20,cols=20,min=0,max=0);
dummy1 = Rand(rows=10,cols=1,min=1.0,max=1.0);
dummy2 = Rand(rows=1,cols=10,min=1.0,max=1.0);

parfor( i in 1:20 )
{                 
   A[:10,i]=dummy1*i;
   A[11:,i]=dummy1*i;
   
   B[i,:10]=dummy2*i;
   B[i,11:]=dummy2*i;
}

#print(C);