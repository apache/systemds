A = Rand(rows=20,cols=20,min=0,max=0);
B = Rand(rows=20,cols=20,min=0,max=0);
C = Rand(rows=20,cols=20,min=0,max=0);
dummy = Rand(rows=1,cols=1,min=1.0,max=1.0);

parfor( i in 1:20 )
{
   A[i:i,1]=dummy*i;
   A[i:i,2]=dummy*i;
   A[i,1]=dummy*i; 
}

#print(C);