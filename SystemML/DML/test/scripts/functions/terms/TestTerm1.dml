# junit test class: dml.test.integration.functions.terms.ScalarMatrixUnaryBinaryTest.java

A = read("$$indir$$a", rows=$$rows$$, cols=$$cols$$, format="text");

W = 1;
W = W + A;
W = W + W;

write(W, "$$outdir$$w", format="text"); 