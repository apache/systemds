dml-path "./scripts/packages:../other/path/to/nowhere";

import dirA::dirAA::testAA as t2;


foo = function(Double C) return ( Double  C)
{
	print("foo in testA.dml in ~/packages/dirA is " + C);
	C = C + 2;
}

bar = function(Double C) return ( Double  C)
{
	print("bar in testA.dml in ~/packages/dirA is " +  C);
	C = C + 3;
}

baz = function(Double C) return ( Double  C)
{
	print("baz in testA.dml in ~/packages/dirA is " + C);
	C = C + 4;
}