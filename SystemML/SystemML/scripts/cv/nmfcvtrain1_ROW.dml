NMFcvtrain = function(a,b) return (wd,hd) {
	zrow = nrow(a);
	numtopics = 4;
	wd = Rand(rows = zrow, cols = numtopics) ;
	
	zcol = ncol(a)
	numtopics = 4;
	hd = Rand(rows = numtopics, cols=zcol) ;

	write( a, "scripts/aRow_output", format="text");
	write( b, "scripts/bRow_output", format="text");


}