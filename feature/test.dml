# Default size = 10M * 10 w/  #distincts per col
# Vary string length from 25 to 500

data = read("feature/data.csv", data_type="frame", format="csv", header=FALSE);


specs = list();
#
jspec1 = read("feature/testspec1.json", data_type="scalar", value_type="string");
specs = append(specs, jspec1);
#
jspec2 = read("feature/testspec2.json", data_type="scalar", value_type="string");

print("("+nrow(data)+", "+ncol(data)+")");
print("data:")
print(toString(data))
allCols = "1";
for (i in 2:ncol(data))
  allCols = allCols+","+i;

recode = allCols;
dummy = allCols;
#jspec1 = "{ ids:true, recode:["+recode+"]}"; #RC

lim = 6;
R = matrix(0, rows=lim, cols=1);
for (i in 1:lim) {
  print("INFO: starting transformencode");
  t1 = time();
  print(toString(data))
  [X, M] = transformencode(target=data, spec=jspec1);
  print("X:")
  print(X);
  print("M:")
  print(M)
  t2 = time();
  R[i,1] = floor((t2-t1)/1000000);
  print("Elapsed time for transformations using SystemDS = "+floor((t2-t1)/1000000)+" millsec");
}

R = order(target=R, by=1);
res = R[1:3,];
write(res, "stringlen_dml.dat", format="csv", sep="\t");
print("("+nrow(X)+", "+ncol(X)+")");