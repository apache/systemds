source("nn/layers/resnetForwardPass.dml") as resnet_fw

m_resnetPredict = function(matrix[double] X, int C, int Hin, int Win,
                   List[Unknown] weights, List[Unknown] bias)
    return (matrix[double] probs) {

        print("ResNet Predict")
        ret = resnet_fw::resnetForward(5)
        probs = matrix(0, rows=5, cols=3)
    }