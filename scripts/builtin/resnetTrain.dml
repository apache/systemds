source("nn/layers/resnetForwardPass.dml") as resnet_fw

m_resnetTrain = function(matrix[double] X, matrix[double] Y,
                 matrix[double] X_val, matrix[double] Y_val,
                 int C, int Hin, int Win, int epochs)
return (List[Unknown] weights, List[Unknown] bias)
{
    print("ResNet Train")
    ret = resnet_fw::resnetForward(5)
    weights = list()
    bias = list()
}