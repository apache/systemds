
fileX = $X;
fileY = $Y;
fileB = $B;
fmtB = "csv";

print ("BEGIN LINEAR REGRESSION g X and Y...");
X = read (fileX);
y = read (fileY);

n = nrow (X);
m = ncol (X);
c = 38;

beta = c*t(X)%*%y;

#beta = matrix(0, rows = m, cols = 1);

#for (i in 1:100)
#{
#    beta += t(X)%*%y;
#    i += 1;
#}

write(beta, fileB, fmtB);

print ("END SCRIPT");
