# Core dependencies
pyyaml>=6.0
numpy>=1.17
tqdm>=4.66
datasets>=2.14
requests>=2.28
psutil>=5.9

# OpenAI backend
openai>=1.0.0
python-dotenv>=1.0

# Optional backends (install as needed):
# mlx-lm>=0.20         # MLX backend - requires Apple Silicon
# vllm>=0.3.0          # vLLM backend - requires NVIDIA GPU
