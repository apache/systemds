# Feed-forward Neural Network Training with federated input matrices X1, X2 and labels Y
X = rbind($X1, $X2);
Y = read($Y);

# Call built-in feed-forward neural network training function
model = ffTrain(X=X, Y=Y, epochs=as.integer($epochs), batch_size=as.integer($batch_size),
               hidden_layers=[128, 64], out_activation="softmax", loss_fcn="cel");

# Write trained model
write(model, $model);