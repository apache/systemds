# Reference Logistic Regression with normal matrices
X1 = read($X1);
X2 = read($X2);
X = rbind(X1, X2);
Y = read($Y);

# Call built-in multi-class logistic regression function
B = multiLogReg(X=X, Y=Y, tol=1e-5, maxi=30, icpt=0);

# Write result
write(B, $B);